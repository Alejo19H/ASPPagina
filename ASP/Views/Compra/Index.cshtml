@model IEnumerable<ASP.Models.compra>

@{
    ViewBag.Title = "Lista Compras";
}

<h2>Lista Compras</h2>

<p>
    @Html.ActionLink("Crear Compra", "Create")
    |
    @Html.ActionLink("Reporte Compra", "Reporte", "ProductoCompra")
</p>
<table class="table">
    <tr>
        <th>
            Fecha Compra
        </th>
        <th>
            Total Compra
        </th>
        <th>
            Usuario
        </th>
        <th>
            Cliente
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.total)
            </td>
            <td>
                @ASP.Controllers.CompraController.NombreUsuario(item.id_usuario)
            </td>
            <td>
                @ASP.Controllers.CompraController.NombreCliente(item.id_cliente)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.id }) |
                @Html.ActionLink("Detalles", "Details", new { id = item.id }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = item.id }, new { @class = "linkDelete" })
            </td>
        </tr>
    }

</table>

@section scripts{
    <script>
        $(".linkDelete").click(function (event)
        {
            if (!confirm('¿Esta seguro de eliminar?'))
            {
                event.preventDefault();
            }
        })
    </script>
}
